# AI2AI 执行状态（State, not logs）

> 只写“当前可执行真相”，不要写流水账。

## 1) 项目当前稳定架构（事实）
- 渲染体系：当前仓库当前实现为 Python 服务层（无前端渲染）。
- 输入体系：服务输入为公众号链接（`link`）与用户思考（`thoughts`）。
- 关键约束（与 Me2AI 一致）：聚焦“给定公众号链接 + 用户思考 → 生成新公众号稿件并进入对应草稿箱”。

---

## 2) 已完成迭代（事实摘要）
- 迭代 1：固化本轮需求与约束到 Me2AI/AI2AI — 已完成。
- 迭代 2：实现最小可执行链路（生成新稿并存入对应草稿箱）— 已通过单元测试验证。

---

## 3) 当前有效约束（事实）
- 不可变约束（来自 Me2AI）：
  - 不自创需求；
  - 不做与本轮目标无关功能扩展；
  - 不进行无关的大规模架构改动。
- 已验证假设（已证据）：
  - 链路可在本地以内存草稿箱完成：
    - 链接模式：解析 `__biz` → 生成稿件 → 写入对应 draftbox；
    - 文件模式：读取源文件内容 + 用户思考整合 → 生成稿件 → 写入指定 draftbox。
- 已知限制 / 技术债（仅事实）：
  - 当前草稿箱为内存仓库实现，未对接真实公众号平台 API；
  - 生成逻辑改为“可注入模型重写器”：构建包含 Me2AI 人设/约束的 prompt，由 rewriter 产出二创正文；文件模式继续支持下载原图、变更图片 MD5 后按原 markdown 位置回填。

---

## 4) 当前执行焦点（事实）
- 正在进行的迭代：无（当前迭代已完成）
- 明确不做的事（本阶段）：
  - 不实现自动发布、多平台分发、数据分析。
- 验收口径（简述）：
  - 输入有效公众号链接与用户思考后，返回新稿并可在对应草稿箱读取到同一稿件。

---

### 迭代 1：更新 Me2AI 与 AI2AI 以对齐新需求 <状态: DONE>
- 范围（scope）：
  - 将用户提出的新需求写入 `spec/Me2AI/需求描述.md` 与 `spec/Me2AI/技术约束.md`；
  - 在 `spec/AI2AI/AI2AI.md` 中记录当前事实状态与下一迭代焦点。
- 完成内容（facts）：
  - 已写入项目目标、核心功能、边界、验收口径与迭代划分；
  - 已写入本轮技术约束与禁止事项；
  - 已同步 AI2AI 状态。
- 关键实现点（facts）：
  - 本次仅文档更新，无代码逻辑变更。
- 验证方式（commands）：
  - `git diff -- spec/Me2AI/需求描述.md spec/Me2AI/技术约束.md spec/AI2AI/AI2AI.md`
- 验证结果（pass/fail + 摘要）：
  - pass：三个文件已按目标完成更新，内容与用户需求一致。
- 已知限制（facts）：
  - 未实现业务代码，后续需在迭代 2 落地功能链路。

### 迭代 2：实现“链接+思考→新稿→草稿箱”最小链路 <状态: DONE>
- 范围（scope）：
  - 新增最小服务代码，支持输入公众号链接与思考后生成新稿并写入对应草稿箱；
  - 使用 TDD：先写失败测试，再实现通过。
- 完成内容（facts）：
  - 新增 `src/wechat_draft_service.py`，包含 `WechatArticleGeneratorService`、`DraftBoxRepository`、`GeneratedArticle`；
  - 新增 `tests/test_wechat_draft_generation.py`，覆盖成功路径与异常输入路径。
- 关键实现点（facts）：
  - 仅接受 `mp.weixin.qq.com/s` 文章链接，并要求包含 `__biz` 参数；
  - 由 `__biz` 生成 `draftbox:{sanitized_biz}` 作为草稿箱标识；
  - 成功生成后将稿件保存到对应草稿箱并可读取验证；
  - 支持 persona 参数（如 科技/AI/新闻/工具）并注入到模型 prompt；
  - 模型 prompt 会加载 `spec/Me2AI/需求描述.md` 与 `spec/Me2AI/技术约束.md` 作为二创人设与约束上下文；
  - 文件模式支持图片处理：下载原图、修改图片 MD5、并按原文图片位置插回新稿内容。
- 验证方式（commands）：
  - `python -m unittest discover -s tests -v`
- 验证结果（pass/fail + 摘要）：
  - pass：10 个测试全部通过（模型重写器调用、Me2AI+persona prompt 注入、文件模式图片下载+MD5变更+位置回填、非法链接/路径、缺失 __biz、缺失文件、空 thoughts、空模型输出拦截）。
- 已知限制（facts）：
  - 未对接外部持久化与真实草稿箱 API，当前为进程内存状态。
